@model FinancialPortal.Models.BankAccounts

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Created")
        </th>
        <th>
            @Html.DisplayName("")
        </th>
        <th>
            @Html.DisplayName("")
        </th>
        <th>
            @Html.DisplayName("")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Transactions)
    {
        <tr>
            <td> 
                @Html.DisplayFor(modelItem => item.Created)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Memo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DipositWithdraw)
            </td>
            <td>
                <a href="@Url.Action("Edit", "Transactions", new { id = item.Id })">Edit</a> |
                <a href="@Url.Action("Delete", "Transactions", new { id = item.Id })">Delete</a>
            </td>
        </tr>
    }

</table>

